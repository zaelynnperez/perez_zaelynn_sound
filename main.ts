while (true) {
    if (input.soundLevel() > 540) {
        light.setPixelColor(0, light.rgb(255, 0, 0))
        pause(400)
        light.clear()
        light.setPixelColor(1, light.rgb(255, 140, 0))
        pause(400)
        light.clear()
        light.setPixelColor(2, light.rgb(255, 255, 0))
        pause(400)
        light.clear()
        light.setPixelColor(3, light.rgb(34, 139, 34))
        pause(400)
        light.clear()
        light.setPixelColor(4, light.rgb(0, 128, 0))
        pause(400)
        light.clear()
        light.setPixelColor(5, light.rgb(66, 242, 245))
        pause(400)
        light.clear()
        light.setPixelColor(6, light.rgb(66, 173, 245))
        pause(400)
        light.clear()
        light.setPixelColor(7, light.rgb(0, 0, 255))
        pause(400)
        light.clear()
        light.setPixelColor(8, light.rgb(48, 25, 52))
        pause(400)
        light.clear()
        light.setPixelColor(9, light.rgb(255, 105, 180))
        pause(400)
        light.clear()
    }
    
}
